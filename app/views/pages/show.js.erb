// Initially I thought an event trigger was necessary to make this work,
// but in reality this was forcing me to submit the form an additional time.
// By using the 'document.ready()' alone, as shown below, it works!
//
// $('#feed_select').bind('submit', function() {
// $(document).on('submit', '#feed_select', function(){

$(document).ready(function(){
  $("#podcast_wrap_inner").html("<%= escape_javascript(render(:partial => 'pages/podcast')) %>");
  $(".spinner").hide();
});